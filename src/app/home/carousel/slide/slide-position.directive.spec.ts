import { SlidePositionDirective } from './slide-position.directive';

describe('SlidePositionDirective', () => {
  it('should create an instance', () => {
    const directive = new SlidePositionDirective();
    expect(directive).toBeTruthy();
  });
});
