export class Category{
    name: string;
    id: string;
    imgUrl: string;
    constructor(name: string, id : string, imgUrl) {
        this.name = name;
        this.id = id;
        this.imgUrl = imgUrl;
      }
}